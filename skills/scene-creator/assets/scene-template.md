# <场景标题>

<一句话描述：这个场景帮用户完成什么，覆盖哪些操作。>

## 前置条件

1. tccli 环境就绪（参考 [../api/README.md](../api/README.md)）
2. 已获取 ZoneId（参考 [../api/zone-discovery.md](../api/zone-discovery.md)）
3. <!-- 场景特有前置条件（如有），如需要用户提供特定信息 -->

## 场景 A：<子场景标题>

**触发**：用户说"<触发短语 1>"、"<触发短语 2>"。

<!-- 描述流程步骤，按顺序列出需要调用的 API 及每一步的目的 -->

**第一步**：<步骤说明>

调用 `ActionName`。

**第二步**：<步骤说明>

调用 `ActionName`。

<!-- 写操作必须有以下安全提示（读操作可省略） -->
> **禁止自动执行**：<操作名称>会<影响说明>。**必须**向用户说明操作内容及影响，等待用户明确确认后才能执行。

**输出建议**：<建议以什么格式向用户展示结果，如表格、摘要等>

## 场景 B：<子场景标题>

**触发**：用户说"<触发短语 1>"、"<触发短语 2>"。

<!-- 重复上述结构 -->

## 输出格式

<!-- 可选：如果场景有固定的输出格式，在此提供模板 -->

```markdown
## <输出标题>

**<关键字段 1>**：xxx
**<关键字段 2>**：xxx

### 结论

<一句话结论>

### 详细数据

| 列 1 | 列 2 | 列 3 |
|---|---|---|
| ... | ... | ... |
```
